<h1>Products</h1>

<div>
  <%= link_to 'New Product', new_product_path %>
</div>

<div id="products">
  <% @products.each do |product| %>
    <div>
      <%= link_to product.name, product %>
    </div>
  <% end %>
</div>
